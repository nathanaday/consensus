{
  "name": "consensus-app",
  "private": true,
  "version": "0.1.0",
  "description": "Local companion app that visualizes the consensus Parquet store as an explorable lineage graph.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
